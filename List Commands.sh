  ls
# With no option lists are files and directories.

  ls -l
# to show long listing information about the file or directory.

  Ls -a
# shows all files regardless of hidden or public.

  Ls -r 
# List all files in reverse order.

Ls -1
# Displays one file per line.
